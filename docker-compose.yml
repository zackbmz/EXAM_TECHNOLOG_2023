version: "3"
services:
  mongodb:
    image: mongo:latest
    ports:
      - 27017:27017
  app:
    build:
      context: ./app
      dockerfile: Dockerfile
    ports:
      - 8763:8763
    depends_on:
      - mongodb
  seeder:
    build:
      context: ./script
      dockerfile: Dockerfile
    depends_on:
      - mongodb
